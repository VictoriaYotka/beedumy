export const coursesSelector = ({ content }) => content.courses;
export const teachersSelector = ({ content }) => content.teachers;
export const newsSelector = ({ content }) => content.news;
export const testimonialsSelector = ({ content }) => content.testimonials;
